.overlay
  position: fixed
  top: 0
  left: 0
  width: 100%
  height: 100%
  display: flex
  flex-direction: column
  overflow: auto
  background-color: rgba(40,40,40,0.2)
  inset: 0px

.content
  margin: 0 auto
  outline: none;
  padding: 100px 0;

.htmlOpen
  overflow: hidden